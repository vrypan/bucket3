All templates, stylesheets, javascript and images (such as logos) can be placed under a directory here, and form a "theme".

It's good practice to clone the default "bucket3" theme and do any modifications there, because the default files may be overwrittern by a future version with newer ones.
